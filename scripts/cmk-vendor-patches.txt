# Intentional patches to vendored Checkmk frontend files.
# Format: <path-relative-to-frontend/src/vendor/cmk>  <one-line rationale>
# Lines beginning with '#' are comments. Empty lines ignored.
#
# Add a new entry whenever you knowingly diverge from upstream — the drift
# check then prints a yellow "patched" row instead of a red "drift" one.
#
# OrbVis-specific stubs and overrides that don't require touching the vendor
# tree live elsewhere:
#   * Component swaps:        frontend/vite.config.ts → CMK_STUBS map
#   * Package shims:          frontend/vite.config.ts → resolve.alias
#                             (e.g. lucide-vue-next → src/cmk-stubs/)
#   * Form-widget overrides:  frontend/src/composables/orbFormComponents.ts
#   * Style overrides:        frontend/src/style.css (CMK component shim section)
# When something *can* live outside the vendor tree, put it there instead of
# adding a manifest entry here.

form/private/FormEditDispatcher/dispatch.ts           Pilot subset of registered forms + opt-in extra-components parameter for OrbVis custom widgets (see composables/orbFormComponents.ts).
components/graphics/RnbwCursor.vue                    Dropped useless initial assignment to supportLinearFiltering; eslint no-useless-assignment was failing the strict pre-commit hook (upstream-PR candidate).
